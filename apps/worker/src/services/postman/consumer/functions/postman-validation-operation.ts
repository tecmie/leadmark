export async function postmanValidationCallback() {
  return;
}

//   async function processCallback(job: Job<any, any, string>): Promise<any> {
//     const childVals = await job.getChildrenValues();
//     // do some stuff
//     // this.logger.log(`Task ${JSON.stringify(childVals)} PREFLIGHT`);
//     this.logger.log(`Token ${token} PREFLIGHT`);
//     return job.data;
//   }
