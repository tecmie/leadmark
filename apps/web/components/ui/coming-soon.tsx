export const ComingSoonTag = ({className}:{className?:string}) =>{
    return(
        <span className={`px-2 text-[10px] border rounded-sm border-link text-link h-max ${className}`}>
        Coming soon
        </span>
    )
}